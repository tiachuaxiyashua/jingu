# Claude Local Instructions

- Follow `AGENTS.md` as the canonical repository instruction file.
- Keep `truth/` as the source of truth for Jingu architecture, harness principles, object models, file protocols, governance, evaluation, and migration direction.
- Do not reintroduce Cyber Editor's old `docs/` worldview or copied root prompts as active Jingu rules.
- For durable repo changes, follow the GitHub synchronization rules in `AGENTS.md`.
- Only apply Superdesign-specific UI workflow when the user explicitly asks for a Superdesign design artifact; save those artifacts under `.superdesign/design_iterations/`.
