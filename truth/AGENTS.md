# Truth Directory Instructions

`truth/` is the active source of truth for Jingu.

- Treat `truth/` as the primary place for future-facing principles, object models, file protocols, harness runtime design, and migration direction.
- Do not resolve conflicts by forcing `truth/` back into Cyber Editor's old `docs/` worldview.
- Do not prematurely convert brainstorms into rigid architecture. Mark open questions clearly.
- When proposing implementation changes for Jingu, trace them back to `truth/` first.
