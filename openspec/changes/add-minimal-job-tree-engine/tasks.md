## 1. OpenSpec And Scope

- [x] 1.1 Create proposal, design, spec, and tasks for the minimal job tree engine.

## 2. Runtime Tree Engine

- [x] 2.1 Add tree projection and active frontier queries over existing jobs.
- [x] 2.2 Add guarded child-job proposal validation and event recording.
- [x] 2.3 Add structured result package validation and submission.
- [x] 2.4 Add parent re-evaluation view for child states, accepted results, gaps, and open questions.

## 3. CLI Workflow

- [x] 3.1 Add CLI commands for child proposal, tree view, frontier view, package submission, and parent re-evaluation.
- [x] 3.2 Add a manual method-validation workflow path using only user-provided method data.

## 4. Verification

- [x] 4.1 Add tests for guarded split failures, true hierarchy, frontier, package submission, and parent re-evaluation.
- [x] 4.2 Add tests that represent method validation without hardcoding method steps into engine behavior.
- [x] 4.3 Run automated tests, compile check, OpenSpec validation, hardcoding scan, and manual CLI sanity checks.
